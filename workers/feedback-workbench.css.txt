:root {
    color-scheme: light;
    --canvas: #f6f8fa;
    --panel: #ffffff;
    --panel-subtle: #f6f8fa;
    --border: #d0d7de;
    --border-muted: #d8dee4;
    --text: #1f2328;
    --muted: #636c76;
    --muted-2: #8c959f;
    --brand: #0969da;
    --brand-hover: #0757b6;
    --brand-soft: #ddf4ff;
    --success: #1a7f37;
    --success-soft: #dafbe1;
    --warning: #9a6700;
    --warning-soft: #fff8c5;
    --danger: #cf222e;
    --danger-soft: #ffebe9;
    --purple: #8250df;
    --purple-soft: #fbefff;
    --shadow: 0 1px 0 rgba(31, 35, 40, 0.04);
    --focus: 0 0 0 3px rgba(9, 105, 218, 0.25);
}

* {
    box-sizing: border-box;
}

html {
    background: var(--canvas);
}

body {
    min-width: 320px;
    margin: 0;
    color: var(--text);
    background: var(--canvas);
    font-family: 'Source Sans 3', 'Microsoft YaHei', system-ui, sans-serif;
    font-size: 14px;
    line-height: 1.5;
}

button,
input,
textarea,
select {
    font: inherit;
}

button,
[role='button'] {
    cursor: pointer;
}

button:focus-visible,
input:focus-visible,
textarea:focus-visible,
select:focus-visible,
summary:focus-visible {
    outline: none;
    box-shadow: var(--focus);
}

svg {
    display: block;
}

.app {
    min-height: 100vh;
}

.topbar {
    position: sticky;
    z-index: 50;
    top: 0;
    display: flex;
    height: 56px;
    align-items: center;
    justify-content: space-between;
    border-bottom: 1px solid var(--border);
    background: rgba(255, 255, 255, 0.96);
    padding: 0 20px;
    backdrop-filter: blur(12px);
}

.brand,
.top-actions,
.top-tabs,
.toolbar-row,
.inline,
.meta-row,
.comment-head,
.composer-actions,
.property-row,
.status-line,
.stat-header,
.settings-title,
.delivery-main {
    display: flex;
    align-items: center;
}

.brand {
    min-width: 260px;
    gap: 10px;
}

.brand-mark {
    display: grid;
    width: 30px;
    height: 30px;
    place-items: center;
    border-radius: 7px;
    color: #ffffff;
    background: #24292f;
}

.brand-copy strong {
    display: block;
    font-size: 15px;
    line-height: 1.1;
}

.brand-copy span {
    color: var(--muted);
    font-size: 12px;
}

.top-tabs {
    align-self: stretch;
    gap: 18px;
}

.top-tab {
    position: relative;
    display: inline-flex;
    align-items: center;
    gap: 7px;
    border: 0;
    color: var(--muted);
    background: none;
    padding: 0 3px;
    font-weight: 600;
}

.top-tab:hover {
    color: var(--text);
}

.top-tab.active {
    color: var(--text);
}

.top-tab.active::after {
    position: absolute;
    right: 0;
    bottom: 0;
    left: 0;
    height: 2px;
    border-radius: 2px 2px 0 0;
    background: #fd8c73;
    content: '';
}

.top-actions {
    justify-content: flex-end;
    min-width: 260px;
    gap: 8px;
}

.button {
    display: inline-flex;
    min-height: 34px;
    align-items: center;
    justify-content: center;
    gap: 7px;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text);
    background: var(--panel-subtle);
    padding: 5px 12px;
    font-weight: 600;
    text-decoration: none;
    transition:
        background 160ms ease,
        border-color 160ms ease,
        color 160ms ease;
}

.button:hover {
    border-color: #afb8c1;
    background: #f3f4f6;
}

.button.primary {
    border-color: rgba(31, 35, 40, 0.15);
    color: #ffffff;
    background: var(--success);
    box-shadow: var(--shadow);
}

.button.primary:hover {
    background: #176f2c;
}

.button.blue {
    border-color: var(--brand);
    color: #ffffff;
    background: var(--brand);
}

.button.blue:hover {
    background: var(--brand-hover);
}

.button:disabled {
    cursor: not-allowed;
    opacity: 0.62;
}

.button.danger {
    color: var(--danger);
    background: #ffffff;
}

.icon-button {
    width: 34px;
    padding: 0;
}

.badge {
    display: inline-flex;
    min-height: 22px;
    align-items: center;
    gap: 5px;
    border: 1px solid transparent;
    border-radius: 999px;
    padding: 1px 8px;
    color: var(--muted);
    background: #eff2f5;
    font-size: 12px;
    font-weight: 600;
    white-space: nowrap;
}

.badge.blue {
    border-color: #b6d7fe;
    color: #0550ae;
    background: #ddf4ff;
}

.badge.green {
    border-color: #a8d5b1;
    color: #116329;
    background: var(--success-soft);
}

.badge.orange {
    border-color: #eac54f;
    color: #7d4e00;
    background: var(--warning-soft);
}

.badge.purple {
    border-color: #d8b9ff;
    color: #6639ba;
    background: var(--purple-soft);
}

.badge.red {
    border-color: #ffb6b9;
    color: #a40e26;
    background: var(--danger-soft);
}

.count {
    display: inline-grid;
    min-width: 20px;
    height: 20px;
    place-items: center;
    border-radius: 10px;
    color: var(--muted);
    background: #eff2f5;
    padding: 0 6px;
    font-size: 11px;
    font-weight: 700;
}

.layout {
    display: grid;
    min-height: calc(100vh - 56px);
    grid-template-columns: 316px minmax(560px, 800px) 294px;
    justify-content: center;
    gap: 24px;
    padding: 24px;
}

.queue {
    position: sticky;
    top: 80px;
    height: calc(100vh - 104px);
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--panel);
    box-shadow: var(--shadow);
}

.queue-head {
    border-bottom: 1px solid var(--border);
    padding: 14px;
}

.toolbar-row {
    justify-content: space-between;
    gap: 10px;
}

.queue-head h2,
.page-heading h1,
.side-card h3,
.settings-title h1,
.settings-card h2 {
    margin: 0;
}

.queue-head h2 {
    font-size: 14px;
}

.queue-head p {
    margin: 1px 0 0;
    color: var(--muted);
    font-size: 12px;
}

.search {
    position: relative;
    margin-top: 12px;
}

.search svg {
    position: absolute;
    top: 9px;
    left: 10px;
    color: var(--muted);
}

.search input {
    width: 100%;
    height: 34px;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text);
    background: var(--panel);
    padding: 6px 10px 6px 32px;
}

.filters {
    display: flex;
    gap: 6px;
    margin-top: 10px;
}

.filter-chip {
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--muted);
    background: #ffffff;
    padding: 3px 8px;
    font-size: 12px;
    font-weight: 600;
}

.filter-chip.active {
    border-color: #a8c7fa;
    color: #0550ae;
    background: var(--brand-soft);
}

.issue-list {
    height: calc(100% - 130px);
    overflow-y: auto;
}

.issue-item {
    position: relative;
    width: 100%;
    border: 0;
    border-bottom: 1px solid #eaeef2;
    color: inherit;
    background: #ffffff;
    padding: 13px 14px;
    text-align: left;
}

.issue-item:hover {
    background: #f6f8fa;
}

.issue-item.active {
    background: #f0f7ff;
}

.issue-item.active::before {
    position: absolute;
    top: 0;
    bottom: 0;
    left: 0;
    width: 3px;
    background: var(--brand);
    content: '';
}

.issue-item-title {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 10px;
    font-weight: 600;
}

.issue-item-copy {
    display: -webkit-box;
    overflow: hidden;
    margin-top: 5px;
    color: var(--muted);
    font-size: 12px;
    -webkit-box-orient: vertical;
    -webkit-line-clamp: 2;
}

.issue-item-meta {
    display: flex;
    align-items: center;
    justify-content: space-between;
    margin-top: 9px;
    color: var(--muted-2);
    font-size: 11px;
}

main {
    min-width: 0;
}

.breadcrumbs {
    display: flex;
    align-items: center;
    gap: 6px;
    margin: 2px 0 8px;
    color: var(--muted);
    font-size: 13px;
}

.page-heading {
    padding-bottom: 18px;
    border-bottom: 1px solid var(--border-muted);
}

.page-heading h1 {
    font-size: clamp(24px, 3vw, 30px);
    font-weight: 600;
    letter-spacing: -0.02em;
    line-height: 1.22;
}

.issue-number {
    color: var(--muted);
    font-weight: 400;
}

.heading-meta {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px;
    margin-top: 10px;
    color: var(--muted);
}

.state-pill {
    display: inline-flex;
    min-height: 30px;
    align-items: center;
    gap: 6px;
    border-radius: 18px;
    color: #ffffff;
    background: var(--success);
    padding: 4px 12px;
    font-weight: 600;
}

.state-pill.attention {
    background: #bf8700;
}

.timeline {
    position: relative;
    padding: 24px 0 4px;
}

.timeline::before {
    position: absolute;
    top: 24px;
    bottom: 0;
    left: 20px;
    width: 2px;
    background: #d8dee4;
    content: '';
}

.timeline-entry {
    position: relative;
    display: grid;
    grid-template-columns: 40px minmax(0, 1fr);
    gap: 12px;
    margin-bottom: 18px;
}

.avatar {
    position: relative;
    z-index: 2;
    display: grid;
    width: 40px;
    height: 40px;
    place-items: center;
    overflow: hidden;
    border: 2px solid var(--canvas);
    border-radius: 50%;
    color: #ffffff;
    background: #24292f;
    font-size: 13px;
    font-weight: 700;
}

.avatar.ai {
    background: linear-gradient(135deg, #6639ba, #0969da);
}

.avatar.event {
    width: 26px;
    height: 26px;
    margin: 7px;
    border: 4px solid var(--canvas);
    color: var(--muted);
    background: #d8dee4;
}

.comment-card {
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--panel);
    box-shadow: var(--shadow);
}

.comment-card.attention {
    border-color: #d4a72c;
}

.comment-head {
    min-height: 42px;
    justify-content: space-between;
    gap: 10px;
    border-bottom: 1px solid var(--border);
    background: var(--panel-subtle);
    padding: 8px 12px;
    color: var(--muted);
    font-size: 13px;
}

.comment-card.attention .comment-head {
    border-color: #e2c55a;
    background: var(--warning-soft);
}

.comment-head strong {
    color: var(--text);
}

.comment-body {
    padding: 15px 16px 16px;
}

.comment-body p {
    margin: 0 0 12px;
}

.comment-body p:last-child {
    margin-bottom: 0;
}

.comment-body ul {
    margin: 8px 0 12px;
    padding-left: 22px;
}

.comment-body li + li {
    margin-top: 5px;
}

.attachment {
    display: inline-flex;
    min-width: 260px;
    align-items: center;
    gap: 10px;
    border: 1px solid var(--border);
    border-radius: 6px;
    background: #ffffff;
    padding: 9px 10px;
}

.attachment-thumb {
    display: grid;
    width: 46px;
    height: 38px;
    place-items: center;
    border-radius: 4px;
    color: var(--brand);
    background: var(--brand-soft);
}

.attachment strong {
    display: block;
    font-size: 13px;
}

.attachment span {
    color: var(--muted);
    font-size: 11px;
}

.result-evidence,
.artifact-evidence {
    margin-top: 14px;
    border-top: 1px solid var(--border-muted);
    padding-top: 14px;
}

.result-title,
.artifact-evidence > div:first-child {
    display: flex;
    align-items: center;
}

.result-title {
    justify-content: space-between;
    gap: 12px;
    margin-bottom: 12px;
}

.result-title > span,
.artifact-evidence span {
    color: var(--muted);
    font-size: 11px;
}

.result-files > strong {
    display: block;
    margin-bottom: 5px;
    font-size: 12px;
}

.result-files ul {
    margin: 0 0 12px;
    padding-left: 20px;
}

.result-files code,
.result-check code {
    overflow-wrap: anywhere;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 11px;
}

.result-checks {
    display: grid;
    gap: 1px;
    overflow: hidden;
    border: 1px solid var(--border-muted);
    border-radius: 6px;
    background: var(--border-muted);
}

.result-check {
    display: grid;
    min-height: 48px;
    grid-template-columns: 22px minmax(0, 1fr) auto;
    align-items: center;
    gap: 9px;
    background: var(--panel);
    padding: 7px 10px;
}

.result-check > span:first-child {
    display: grid;
    width: 20px;
    height: 20px;
    place-items: center;
    border-radius: 50%;
    color: var(--success);
    background: var(--success-soft);
    font-weight: 700;
}

.result-check.failed > span:first-child {
    color: var(--danger);
    background: var(--danger-soft);
}

.result-check.skipped > span:first-child {
    color: var(--muted);
    background: var(--panel-subtle);
}

.result-check strong,
.result-check code {
    display: block;
}

.result-check > span:last-child {
    color: var(--success);
    font-size: 11px;
    font-weight: 600;
}

.result-check.failed > span:last-child,
.visual-status.failed,
.result-warning {
    color: var(--danger);
}

.result-check.skipped > span:last-child {
    color: var(--muted);
}

.visual-status {
    margin-top: 10px !important;
    color: var(--success);
    font-size: 12px;
    font-weight: 600;
}

.artifact-evidence {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 10px 14px;
}

.artifact-evidence > div:first-child {
    min-width: 0;
    align-items: baseline;
    gap: 8px;
}

.artifact-link {
    color: var(--brand);
    font-size: 12px;
    font-weight: 600;
    text-decoration: none;
}

.artifact-link:hover {
    text-decoration: underline;
}

.artifact-preview-link {
    grid-column: 1 / -1;
    display: block;
    width: min(100%, 720px);
    overflow: hidden;
    border: 1px solid var(--border-muted);
    border-radius: 6px;
    background: var(--panel-subtle);
}

.artifact-preview-link img {
    display: block;
    width: 100%;
    height: auto;
    max-height: 420px;
    object-fit: contain;
}

.event-row {
    display: flex;
    min-height: 40px;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    color: var(--muted);
    padding: 3px 0;
    font-size: 13px;
}

.event-row strong {
    color: var(--text);
    font-weight: 600;
}

.evidence {
    margin-top: 12px;
    border: 1px solid var(--border-muted);
    border-radius: 6px;
    background: #f6f8fa;
}

.evidence summary {
    display: flex;
    min-height: 40px;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    border-radius: 6px;
    padding: 8px 11px;
    font-weight: 600;
    list-style: none;
    cursor: pointer;
}

.evidence summary::-webkit-details-marker {
    display: none;
}

.evidence-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 8px;
    border-top: 1px solid var(--border-muted);
    padding: 10px;
}

.evidence-item {
    min-height: 76px;
    border: 1px solid var(--border-muted);
    border-radius: 5px;
    background: #ffffff;
    padding: 9px;
}

.evidence-item span {
    display: block;
    color: var(--muted);
    font-size: 11px;
}

.evidence-item strong {
    display: block;
    margin-top: 3px;
    font-size: 12px;
    line-height: 1.35;
}

.decision-box {
    display: grid;
    grid-template-columns: 22px 1fr;
    gap: 10px;
    margin-top: 12px;
    border: 1px solid #e2c55a;
    border-radius: 6px;
    color: #5f4507;
    background: #fffdf0;
    padding: 11px 12px;
}

.decision-box strong {
    display: block;
    color: #3b2b05;
}

.decision-box p {
    margin: 2px 0 0;
    color: #6e550c;
    font-size: 13px;
}

.composer {
    position: relative;
    display: grid;
    grid-template-columns: 40px minmax(0, 1fr);
    gap: 12px;
    margin-top: 8px;
}

.composer-panel {
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--panel);
    box-shadow: var(--shadow);
}

.composer-tabs {
    display: flex;
    min-height: 42px;
    align-items: flex-end;
    gap: 18px;
    border-bottom: 1px solid var(--border);
    padding: 0 14px;
}

.composer-tab {
    align-self: stretch;
    border: 0;
    border-bottom: 2px solid transparent;
    color: var(--muted);
    background: transparent;
    padding: 0 2px;
    font-weight: 600;
}

.composer-tab.active {
    border-bottom-color: #fd8c73;
    color: var(--text);
}

.composer-editor {
    padding: 12px;
}

.mention-bar {
    display: flex;
    align-items: center;
    gap: 7px;
    margin-bottom: 8px;
}

.mention-button {
    border: 1px solid #b6d7fe;
    border-radius: 999px;
    color: #0550ae;
    background: var(--brand-soft);
    padding: 3px 9px;
    font-size: 12px;
    font-weight: 600;
}

.mention-hint {
    color: var(--muted);
    font-size: 12px;
}

.composer textarea {
    display: block;
    width: 100%;
    min-height: 112px;
    resize: vertical;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text);
    background: #ffffff;
    padding: 10px 11px;
    line-height: 1.55;
}

.composer-actions {
    justify-content: space-between;
    gap: 12px;
    margin-top: 10px;
}

.reply-mode {
    height: 34px;
    max-width: 220px;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text);
    background: var(--panel-subtle);
    padding: 4px 30px 4px 10px;
    font-weight: 600;
}

.reply-attachments {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-top: 10px;
    flex-wrap: wrap;
}

.attachment-picker {
    min-height: 30px;
    padding: 5px 10px;
    cursor: pointer;
}

.reply-attachment-list {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
    margin-top: 8px;
}

.reply-attachment-chip {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    max-width: 100%;
    padding: 4px 8px;
    border: 1px solid var(--line, #d9e0e7);
    border-radius: 6px;
    color: var(--ink, #27313b);
    font-size: 12px;
}

.reply-attachment-chip strong {
    overflow: hidden;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.reply-success {
    display: none;
    align-items: center;
    gap: 7px;
    color: var(--success);
    font-size: 13px;
    font-weight: 600;
}

aside {
    min-width: 0;
}

.aside-sticky {
    position: sticky;
    top: 80px;
    display: grid;
    gap: 14px;
}

.side-card {
    overflow: hidden;
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--panel);
    box-shadow: var(--shadow);
}

.side-card-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 10px;
    border-bottom: 1px solid var(--border);
    background: var(--panel-subtle);
    padding: 10px 12px;
}

.side-card h3 {
    font-size: 13px;
}

.side-card-body {
    padding: 12px;
}

.next-action {
    border-color: #d4a72c;
}

.next-action .side-card-head {
    border-color: #e2c55a;
    background: var(--warning-soft);
}

.next-action-copy {
    color: var(--muted);
    font-size: 13px;
}

.next-action-copy strong {
    display: block;
    margin-bottom: 3px;
    color: var(--text);
    font-size: 14px;
}

.side-actions {
    display: grid;
    gap: 7px;
    margin-top: 12px;
}

.side-actions .button {
    width: 100%;
}

.property-list {
    display: grid;
}

.property-row {
    min-height: 42px;
    justify-content: space-between;
    gap: 12px;
    border-bottom: 1px solid #eaeef2;
    padding: 8px 0;
}

.property-row:last-child {
    border-bottom: 0;
}

.property-row > span:first-child {
    color: var(--muted);
    font-size: 12px;
}

.property-value {
    text-align: right;
    font-weight: 600;
}

.avatar-mini {
    display: inline-grid;
    width: 20px;
    height: 20px;
    place-items: center;
    border-radius: 50%;
    color: #ffffff;
    background: #24292f;
    font-size: 9px;
}

.automation-health {
    display: grid;
    gap: 9px;
}

.status-line {
    justify-content: space-between;
    gap: 8px;
    color: var(--muted);
    font-size: 12px;
}

.status-value {
    display: inline-flex;
    align-items: center;
    gap: 6px;
    color: var(--text);
    font-weight: 600;
}

.status-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
    background: var(--success);
    box-shadow: 0 0 0 3px var(--success-soft);
}

.status-dot.warn {
    background: #bf8700;
    box-shadow: 0 0 0 3px var(--warning-soft);
}

.cost-note {
    border-radius: 6px;
    color: #0550ae;
    background: var(--brand-soft);
    padding: 8px 9px;
    font-size: 12px;
}

.mobile-queue-button {
    display: none;
}

.settings-view {
    display: none;
    width: min(1180px, calc(100% - 40px));
    margin: 0 auto;
    padding: 28px 0 48px;
}

.settings-view.active {
    display: block;
}

.issue-view.hidden {
    display: none;
}

.hidden {
    display: none !important;
}

.settings-title {
    justify-content: space-between;
    gap: 20px;
    margin-bottom: 22px;
}

.settings-title h1 {
    font-size: 28px;
    line-height: 1.2;
}

.settings-title p {
    margin: 4px 0 0;
    color: var(--muted);
}

.settings-heading {
    display: flex;
    align-items: center;
    gap: 10px;
    flex-wrap: wrap;
}

.settings-heading .badge {
    gap: 6px;
}

.settings-heading .status-dot {
    width: 7px;
    height: 7px;
    box-shadow: none;
}

.settings-title > .button {
    min-width: 104px;
}

.stats {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 12px;
    margin-bottom: 18px;
}

.stat-card,
.settings-card {
    border: 1px solid var(--border);
    border-radius: 8px;
    background: var(--panel);
    box-shadow: var(--shadow);
}

.stat-card {
    padding: 14px 15px;
}

.stat-header {
    justify-content: space-between;
    color: var(--muted);
    font-size: 12px;
}

.stat-value {
    margin-top: 7px;
    font-size: 25px;
    font-weight: 600;
    letter-spacing: -0.02em;
}

.stat-foot {
    margin-top: 2px;
    color: var(--muted);
    font-size: 12px;
}

.settings-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.55fr) minmax(320px, 0.85fr);
    gap: 18px;
    align-items: start;
}

.settings-column {
    display: grid;
    gap: 18px;
}

.automation-settings-grid {
    grid-template-columns: minmax(0, 1.35fr) minmax(300px, 0.75fr);
    gap: 16px;
}

.settings-card-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 16px;
    border-bottom: 1px solid var(--border);
    padding: 14px 16px;
}

.settings-card h2 {
    font-size: 15px;
}

.settings-card-head p {
    margin: 2px 0 0;
    color: var(--muted);
    font-size: 12px;
}

.settings-card-head.compact {
    align-items: center;
    padding-block: 12px;
}

.settings-card-body {
    padding: 16px;
}

.field {
    display: grid;
    gap: 6px;
    margin-bottom: 14px;
}

.field:last-child {
    margin-bottom: 0;
}

.field label,
.field-label {
    color: var(--text);
    font-size: 12px;
    font-weight: 600;
}

.field-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
}

.input-group {
    display: flex;
    gap: 8px;
}

.input-group .button {
    flex: 0 0 auto;
    white-space: nowrap;
}

.field input[type='text'],
.field input[type='url'],
.field input[type='password'] {
    width: 100%;
    height: 38px;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text);
    background: #ffffff;
    padding: 7px 10px;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 12px;
}

.help {
    color: var(--muted);
    font-size: 11px;
}

.connection-status {
    display: inline-flex;
    align-items: center;
    gap: 5px;
    width: max-content;
    color: var(--muted);
    font-size: 11px;
}

.connection-status.success {
    color: var(--success);
}

.connection-status.error {
    color: var(--danger);
}

.connection-status svg {
    flex: 0 0 auto;
}

.event-checks {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 8px;
}

.check-card {
    display: grid;
    grid-template-columns: 18px 1fr;
    gap: 8px;
    border: 1px solid var(--border-muted);
    border-radius: 6px;
    padding: 10px;
    cursor: pointer;
    transition:
        border-color 160ms ease,
        background 160ms ease;
}

.check-card:hover,
.check-card:focus-within {
    border-color: #8c959f;
}

.check-card:has(input:checked) {
    border-color: #b6d7ff;
    background: #f6faff;
}

.check-card input {
    width: 16px;
    height: 16px;
    margin: 2px 0 0;
    accent-color: var(--brand);
}

.check-card strong {
    display: block;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 11px;
}

.check-card span {
    color: var(--muted);
    font-size: 11px;
}

.advanced-details {
    margin-bottom: 16px;
    overflow: hidden;
    border: 1px solid var(--border-muted);
    border-radius: 6px;
    background: var(--panel-subtle);
}

.advanced-details summary {
    display: flex;
    min-height: 44px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 8px 10px;
    font-size: 12px;
    font-weight: 600;
    list-style: none;
    cursor: pointer;
}

.advanced-details summary::-webkit-details-marker,
.delivery-card summary::-webkit-details-marker {
    display: none;
}

.advanced-details[open] summary {
    border-bottom: 1px solid var(--border-muted);
}

.advanced-details-body {
    padding: 12px;
}

.advanced-details-body .field {
    margin-bottom: 8px;
}

.advanced-details-body .help {
    line-height: 1.55;
}

.pipeline {
    display: grid;
    grid-template-columns: 1fr 32px 1fr 32px 1fr;
    align-items: center;
    gap: 4px;
}

.pipeline-node {
    display: grid;
    min-height: 102px;
    place-items: center;
    border: 1px solid var(--border-muted);
    border-radius: 7px;
    background: var(--panel-subtle);
    padding: 10px;
    text-align: center;
}

.pipeline-node svg {
    color: var(--brand);
}

.pipeline-node strong {
    display: block;
    margin-top: 7px;
    font-size: 12px;
}

.pipeline-node span {
    color: var(--muted);
    font-size: 10px;
}

.pipeline-arrow {
    display: grid;
    place-items: center;
    color: var(--muted-2);
}

.delivery-list {
    display: grid;
    gap: 0;
}

.delivery {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: 10px;
    border-bottom: 1px solid #eaeef2;
    padding: 11px 0;
}

.delivery:last-child {
    border-bottom: 0;
    padding-bottom: 0;
}

.delivery:first-child {
    padding-top: 0;
}

.delivery-main {
    min-width: 0;
    gap: 8px;
}

.delivery-icon {
    display: grid;
    width: 24px;
    height: 24px;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 50%;
    color: var(--success);
    background: var(--success-soft);
}

.delivery-copy {
    min-width: 0;
}

.delivery-copy strong {
    display: block;
    overflow: hidden;
    text-overflow: ellipsis;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 11px;
    white-space: nowrap;
}

.delivery-copy span,
.delivery-time {
    color: var(--muted);
    font-size: 11px;
}

.delivery-card {
    overflow: hidden;
}

.delivery-card summary {
    display: flex;
    min-height: 54px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 12px 16px;
    list-style: none;
    cursor: pointer;
}

.delivery-summary {
    display: grid;
    gap: 2px;
}

.delivery-summary strong {
    font-size: 15px;
}

.delivery-summary span {
    color: var(--muted);
    font-size: 11px;
}

.summary-chevron {
    color: var(--muted);
    transition: transform 160ms ease;
}

.delivery-card[open] .summary-chevron {
    transform: rotate(180deg);
}

.advanced-details[open] .summary-chevron {
    transform: rotate(180deg);
}

.delivery-card[open] summary {
    border-bottom: 1px solid var(--border);
}

.toggle-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
    border-bottom: 1px solid #eaeef2;
    padding: 11px 0;
}

.toggle-row:last-child {
    border-bottom: 0;
}

.toggle-copy strong {
    display: block;
    font-size: 13px;
}

.toggle-copy span {
    color: var(--muted);
    font-size: 11px;
}

.switch {
    position: relative;
    width: 44px;
    height: 44px;
    flex: 0 0 auto;
    border: 0;
    border-radius: 22px;
    background: transparent;
    padding: 0;
}

.switch::before {
    position: absolute;
    top: 10px;
    left: 0;
    width: 44px;
    height: 24px;
    border-radius: 12px;
    background: #afb8c1;
    content: '';
    transition: background 160ms ease;
}

.switch::after {
    position: absolute;
    top: 13px;
    left: 3px;
    width: 18px;
    height: 18px;
    border-radius: 50%;
    background: #ffffff;
    content: '';
    transition: transform 160ms ease;
}

.switch.on {
    background: transparent;
}

.switch.on::before {
    background: var(--success);
}

.switch.on::after {
    transform: translateX(20px);
}

.executor-row {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 14px;
}

.executor-main {
    display: flex;
    min-width: 0;
    align-items: center;
    gap: 10px;
}

.executor-icon {
    display: grid;
    width: 36px;
    height: 36px;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 8px;
    color: var(--brand);
    background: var(--brand-soft);
}

.executor-copy strong,
.executor-copy span {
    display: block;
}

.executor-copy strong {
    font-size: 13px;
}

.executor-copy span {
    margin-top: 1px;
    color: var(--muted);
    font-size: 11px;
}

.runner-title-actions {
    display: flex;
    align-items: center;
    gap: 10px;
}

.runner-title-actions > span {
    color: var(--muted);
    font-size: 11px;
}

.runner-settings-grid {
    display: grid;
    grid-template-columns: minmax(0, 1.35fr) minmax(280px, 0.65fr);
    align-items: start;
    gap: 16px;
}

.runner-settings-grid .button,
.runner-title-actions .button,
.runner-settings-grid .field input,
.runner-select-row select {
    min-height: 44px;
}

.runner-provider-grid {
    gap: 10px;
}

.runner-provider-grid .provider-card {
    display: flex;
    min-width: 0;
    flex-direction: column;
    background: #ffffff;
    transition:
        border-color 160ms ease,
        box-shadow 160ms ease;
}

.runner-provider-grid .provider-card:hover {
    border-color: #8c959f;
}

.provider-state,
.provider-buttons {
    display: flex;
    align-items: center;
    gap: 6px;
}

.provider-summary {
    min-height: 36px;
    margin: 12px 0;
    color: var(--muted);
    font-size: 11px;
    line-height: 1.55;
}

.runner-provider-grid .provider-actions {
    align-items: center;
    flex-wrap: wrap;
}

.runner-provider-grid .provider-buttons {
    margin-left: auto;
}

.provider-history {
    width: 100%;
    margin-top: 12px;
    border-top: 1px solid var(--line);
}

.provider-history > summary {
    display: flex;
    min-height: 44px;
    align-items: center;
    gap: 8px;
    cursor: pointer;
    list-style: none;
    color: var(--text);
    font-size: 12px;
    font-weight: 600;
}

.provider-history > summary::-webkit-details-marker {
    display: none;
}

.provider-history > summary .provider-history-count {
    margin-left: auto;
    color: var(--muted);
    font-size: 11px;
    font-weight: 400;
}

.provider-history[open] > summary .summary-chevron {
    transform: rotate(180deg);
}

.provider-history-body {
    display: grid;
    gap: 8px;
    padding: 0 0 10px;
}

.provider-history-row {
    min-width: 0;
    border: 1px solid var(--border-muted);
    border-radius: 6px;
    background: var(--panel-subtle);
    padding: 9px 10px;
}

.provider-history-head,
.provider-history-meta {
    display: flex;
    min-width: 0;
    align-items: center;
    gap: 8px;
    flex-wrap: wrap;
}

.provider-history-head strong {
    color: var(--text);
    font-size: 11px;
}

.provider-history-head > span,
.provider-history-meta,
.provider-history-commit,
.provider-history-url,
.provider-history-empty {
    color: var(--muted);
    font-size: 10px;
    line-height: 1.5;
    overflow-wrap: anywhere;
}

.provider-history-head > span {
    margin-left: auto;
}

.provider-history-meta {
    margin-top: 4px;
}

.provider-history-meta span {
    max-width: 100%;
    overflow-wrap: anywhere;
}

.provider-history-meta span + span::before {
    content: '·';
    margin-right: 8px;
    color: var(--muted-2);
}

.provider-history-commit,
.provider-history-url {
    margin-top: 3px;
}

.provider-history-empty {
    padding: 8px 0 12px;
}

.provider-config {
    width: 100%;
    margin: 12px 0 0;
}

.provider-config .advanced-details-body {
    background: #ffffff;
}

.runner-secret-row {
    display: flex;
    min-height: 44px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
}

.runner-secret-row span,
.runner-secret-row strong {
    display: block;
}

.runner-secret-row span {
    color: var(--muted);
    font-size: 10px;
}

.runner-secret-row strong {
    margin-top: 2px;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 10px;
}

.runner-route-form {
    display: grid;
    gap: 0;
}

.runner-select-row,
.runner-route-row {
    display: flex;
    min-height: 54px;
    align-items: center;
    justify-content: space-between;
    gap: 16px;
    border-bottom: 1px solid #eaeef2;
    padding: 8px 0;
}

.runner-select-row:first-child {
    padding-top: 0;
}

.runner-route-row:last-child {
    border-bottom: 0;
    padding-bottom: 0;
}

.runner-select-row > span > strong,
.runner-select-row > span > span,
.runner-route-row > span > strong,
.runner-route-row > span > small {
    display: block;
}

.runner-select-row > span > strong,
.runner-route-row strong {
    font-size: 12px;
}

.runner-select-row > span > span,
.runner-route-row small {
    margin-top: 2px;
    color: var(--muted);
    font-size: 10px;
}

.runner-select-row select {
    min-width: 150px;
    height: 38px;
    border: 1px solid var(--border);
    border-radius: 6px;
    color: var(--text);
    background: #ffffff;
    padding: 6px 30px 6px 10px;
    font: inherit;
    font-size: 12px;
    cursor: pointer;
}

.runner-health-list {
    display: grid;
    gap: 0;
}

.runner-health-list > div {
    display: flex;
    min-height: 42px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    border-bottom: 1px solid #eaeef2;
    padding: 8px 0;
}

.runner-health-list > div:first-child {
    padding-top: 0;
}

.runner-health-list > div:last-child {
    border-bottom: 0;
    padding-bottom: 0;
}

.runner-health-list span {
    color: var(--muted);
    font-size: 11px;
}

.runner-health-list strong {
    font-size: 11px;
    text-align: right;
}

.runner-health-list .inline-success {
    color: var(--success);
}

.runner-advanced-card {
    overflow: hidden;
}

.runner-advanced-card > summary {
    display: flex;
    min-height: 64px;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    padding: 12px 16px;
    list-style: none;
    cursor: pointer;
}

.runner-advanced-card > summary::-webkit-details-marker {
    display: none;
}

.runner-advanced-card > summary strong,
.runner-advanced-card > summary small {
    display: block;
}

.runner-advanced-card > summary strong {
    font-size: 15px;
}

.runner-advanced-card > summary small {
    margin-top: 2px;
    color: var(--muted);
    font-size: 11px;
}

.runner-advanced-card[open] > summary {
    border-bottom: 1px solid var(--border);
}

.runner-advanced-card[open] .summary-chevron {
    transform: rotate(180deg);
}

.runner-contract > span {
    display: block;
    margin-bottom: 7px;
    font-size: 12px;
    font-weight: 600;
}

.runner-contract > div {
    display: flex;
    flex-wrap: wrap;
    gap: 6px;
}

.runner-contract code {
    border-radius: 5px;
    color: #0550ae;
    background: var(--brand-soft);
    padding: 4px 6px;
    font-size: 10px;
}

.runner-sdk-note {
    margin-top: 14px;
}

/* §19.5 graded autonomy block. Kept inside 高级设置 so the executor page's first
   screen stays focused on selection, connection and routing. */
.runner-autodeliver {
    margin-top: 16px;
    padding-top: 14px;
    border-top: 1px solid var(--line);
    min-width: 0;
}

.runner-autodeliver-head {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 12px;
    min-width: 0;
}

.runner-autodeliver-head strong {
    display: block;
    font-size: 13px;
}

.runner-autodeliver-head small {
    display: block;
    margin-top: 2px;
    color: var(--muted);
    font-size: 11px;
    overflow-wrap: anywhere;
}

.runner-autodeliver-status {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 8px;
    margin-top: 10px;
    color: var(--muted);
    font-size: 12px;
}

.runner-autodeliver-actions {
    display: flex;
    flex-wrap: wrap;
    align-items: center;
    gap: 10px;
    margin-top: 12px;
}

/* The E2E asserts a 44px minimum hit area on every visible control at 375px. */
.runner-autodeliver-actions > button {
    min-height: 44px;
}

.runner-autodeliver-detail {
    margin-top: 12px;
}

.runner-autodeliver-detail > summary {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
    min-height: 44px;
    cursor: pointer;
    list-style: none;
    color: var(--muted);
}

.runner-autodeliver-detail > summary::-webkit-details-marker {
    display: none;
}

.runner-autodeliver-detail[open] .summary-chevron {
    transform: rotate(180deg);
}

.runner-autodeliver-detail-body {
    padding-top: 10px;
    min-width: 0;
}

.runner-autodeliver-detail-body textarea {
    width: 100%;
    box-sizing: border-box;
    resize: vertical;
}

.recommendation {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 18px;
    border: 1px solid #54aeff;
    border-radius: 8px;
    background: #f6f8fa;
    padding: 16px;
    margin-bottom: 18px;
}

.recommendation-copy {
    display: flex;
    align-items: flex-start;
    gap: 12px;
}

.recommendation-icon {
    display: grid;
    width: 34px;
    height: 34px;
    flex: 0 0 auto;
    place-items: center;
    border-radius: 7px;
    color: var(--brand);
    background: var(--brand-soft);
}

.recommendation h2 {
    font-size: 15px;
}

.recommendation p {
    max-width: 760px;
    margin: 4px 0 0;
    color: var(--muted);
    font-size: 12px;
    line-height: 1.65;
}

.flow-grid {
    display: grid;
    grid-template-columns:
        minmax(120px, 1fr) 24px minmax(120px, 1fr) 24px
        minmax(120px, 1fr) 24px minmax(120px, 1fr) 24px
        minmax(120px, 1fr);
    align-items: center;
    gap: 3px;
}

.flow-node {
    min-height: 116px;
    border: 1px solid var(--border-muted);
    border-radius: 7px;
    background: var(--panel-subtle);
    padding: 13px 10px;
    text-align: center;
}

.flow-node.highlight {
    border-color: #54aeff;
    background: var(--brand-soft);
}

.flow-node svg {
    color: var(--brand);
}

.flow-node strong {
    display: block;
    margin-top: 8px;
    font-size: 12px;
}

.flow-node span {
    display: block;
    margin-top: 3px;
    color: var(--muted);
    font-size: 10px;
    line-height: 1.45;
}

.flow-arrow {
    display: grid;
    place-items: center;
    color: var(--muted-2);
}

.provider-grid {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 12px;
}

.provider-card {
    border: 1px solid var(--border-muted);
    border-radius: 7px;
    background: var(--panel-subtle);
    padding: 14px;
}

.provider-card.selected {
    border-color: #54aeff;
    box-shadow: inset 3px 0 0 var(--brand);
}

.provider-head {
    display: flex;
    align-items: flex-start;
    justify-content: space-between;
    gap: 10px;
}

.provider-title {
    display: flex;
    min-width: 0;
    flex: 1 1 auto;
    align-items: center;
    gap: 9px;
}

.provider-title > span:last-child {
    min-width: 0;
    overflow-wrap: anywhere;
}

.provider-icon {
    display: grid;
    width: 32px;
    height: 32px;
    flex: 0 0 auto;
    place-items: center;
    border: 1px solid var(--border-muted);
    border-radius: 7px;
    color: var(--text);
    background: #ffffff;
}

.provider-title strong {
    display: block;
    font-size: 13px;
}

.provider-title span {
    color: var(--muted);
    font-size: 11px;
}

.provider-details {
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 9px;
    margin: 14px 0;
}

.provider-detail {
    border-top: 1px solid #eaeef2;
    padding-top: 8px;
}

.provider-detail span {
    display: block;
    color: var(--muted);
    font-size: 10px;
}

.provider-detail strong {
    display: block;
    overflow: hidden;
    margin-top: 2px;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 10px;
    text-overflow: ellipsis;
    white-space: nowrap;
}

.provider-actions {
    display: flex;
    align-items: center;
    justify-content: space-between;
    gap: 8px;
}

.provider-note {
    color: var(--muted);
    font-size: 10px;
}

.route-list,
.contract-list,
.run-list {
    display: grid;
    gap: 0;
}

.route-row {
    display: grid;
    grid-template-columns: 130px minmax(0, 1fr) auto;
    align-items: center;
    gap: 12px;
    border-bottom: 1px solid #eaeef2;
    padding: 11px 0;
}

.route-row:first-child,
.contract-row:first-child,
.run-row:first-child {
    padding-top: 0;
}

.route-row:last-child,
.contract-row:last-child,
.run-row:last-child {
    border-bottom: 0;
    padding-bottom: 0;
}

.mention-code,
.event-code {
    width: max-content;
    border-radius: 5px;
    color: #0550ae;
    background: var(--brand-soft);
    padding: 3px 6px;
    font-family: ui-monospace, SFMono-Regular, Consolas, monospace;
    font-size: 10px;
    font-weight: 600;
}

.route-copy strong,
.contract-copy strong,
.run-copy strong {
    display: block;
    font-size: 12px;
}

.route-copy span,
.contract-copy span,
.run-copy span {
    color: var(--muted);
    font-size: 10px;
}

.contract-row {
    display: grid;
    grid-template-columns: 118px minmax(0, 1fr);
    align-items: center;
    gap: 11px;
    border-bottom: 1px solid #eaeef2;
    padding: 10px 0;
}

.run-row {
    display: grid;
    grid-template-columns: 20px minmax(0, 1fr) auto;
    align-items: start;
    gap: 9px;
    border-bottom: 1px solid #eaeef2;
    padding: 10px 0;
}

.run-dot {
    width: 9px;
    height: 9px;
    margin: 4px 0 0 4px;
    border-radius: 50%;
    background: var(--success);
    box-shadow: 0 0 0 3px var(--success-soft);
}

.run-dot.waiting {
    background: #bf8700;
    box-shadow: 0 0 0 3px var(--warning-soft);
}

.run-time {
    color: var(--muted);
    font-size: 10px;
    white-space: nowrap;
}

.policy-callout {
    margin-top: 12px;
    border-left: 3px solid var(--warning);
    border-radius: 0 5px 5px 0;
    color: #6e4b00;
    background: var(--warning-soft);
    padding: 9px 10px;
    font-size: 11px;
    line-height: 1.55;
}

.toast {
    position: fixed;
    z-index: 100;
    right: 20px;
    bottom: 20px;
    display: none;
    align-items: center;
    gap: 8px;
    border: 1px solid #2da44e;
    border-radius: 7px;
    color: #ffffff;
    background: #24292f;
    padding: 10px 13px;
    box-shadow: 0 8px 28px rgba(31, 35, 40, 0.2);
    font-weight: 600;
}

@media (max-width: 1220px) {
    .layout {
        grid-template-columns: 286px minmax(0, 760px);
    }

    aside {
        display: none;
    }
}

@media (max-width: 920px) {
    .brand {
        min-width: 0;
    }

    .brand-copy span {
        display: none;
    }

    .top-actions {
        min-width: 0;
    }

    .layout {
        display: block;
        padding: 18px;
    }

    .queue {
        position: fixed;
        z-index: 80;
        top: 56px;
        bottom: 0;
        left: 0;
        width: min(340px, calc(100vw - 48px));
        height: auto;
        border-radius: 0 8px 8px 0;
        transform: translateX(-102%);
        transition: transform 180ms ease;
        box-shadow: 12px 0 30px rgba(31, 35, 40, 0.18);
    }

    .queue.open {
        transform: translateX(0);
    }

    .mobile-queue-button {
        display: inline-flex;
    }

    .stats {
        grid-template-columns: repeat(2, 1fr);
    }

    .settings-grid {
        grid-template-columns: 1fr;
    }

    .runner-settings-grid {
        grid-template-columns: 1fr;
    }

    .flow-grid {
        grid-template-columns: 1fr;
    }

    .flow-arrow {
        transform: rotate(90deg);
    }
}

@media (max-width: 680px) {
    body {
        font-size: 14px;
    }

    .topbar {
        height: 56px;
        gap: 6px;
        padding: 0 12px;
    }

    .brand-mark {
        width: 28px;
        height: 28px;
    }

    .brand-copy {
        display: none;
    }

    .top-tabs {
        margin-left: auto;
        gap: 8px;
    }

    .top-tab {
        font-size: 13px;
    }

    .top-tab svg {
        display: none;
    }

    .top-tab,
    .button,
    .filter-chip,
    .mention-button,
    .composer-tab {
        min-height: 44px;
    }

    .top-tab .count {
        display: none;
    }

    .top-actions .button:not(.mobile-queue-button) span {
        display: none;
    }

    .layout {
        min-height: calc(100vh - 56px);
        padding: 14px 12px 32px;
    }

    .breadcrumbs {
        margin-top: 0;
    }

    .page-heading h1 {
        font-size: 24px;
    }

    .timeline::before {
        left: 16px;
    }

    .timeline-entry,
    .composer {
        grid-template-columns: 32px minmax(0, 1fr);
        gap: 9px;
    }

    .avatar {
        width: 32px;
        height: 32px;
        font-size: 11px;
    }

    .avatar.event {
        width: 22px;
        height: 22px;
        margin: 5px;
    }

    .comment-body {
        padding: 13px;
    }

    .comment-head {
        align-items: flex-start;
        flex-direction: column;
        padding: 8px 11px;
    }

    .evidence-grid {
        grid-template-columns: 1fr;
    }

    .attachment {
        min-width: 0;
        width: 100%;
    }

    .composer-actions {
        align-items: stretch;
        flex-direction: column;
    }

    .reply-mode,
    .composer-actions .button {
        width: 100%;
        max-width: none;
    }

    .settings-view {
        width: calc(100% - 24px);
        padding-top: 18px;
    }

    .settings-title {
        align-items: flex-start;
        flex-direction: column;
        gap: 12px;
        margin-bottom: 16px;
    }

    .settings-title h1 {
        font-size: 24px;
    }

    .settings-title > .button {
        width: 100%;
    }

    .runner-title-actions {
        width: 100%;
        align-items: stretch;
        flex-direction: column;
        gap: 6px;
    }

    .runner-title-actions .button {
        width: 100%;
    }

    .stats {
        grid-template-columns: 1fr 1fr;
    }

    .input-group {
        display: grid;
        grid-template-columns: 1fr;
    }

    .input-group .button {
        width: 100%;
    }

    .event-checks {
        grid-template-columns: 1fr;
    }

    .pipeline {
        grid-template-columns: 1fr;
    }

    .pipeline-arrow {
        transform: rotate(90deg);
    }

    .provider-grid {
        grid-template-columns: 1fr;
    }

    .runner-provider-grid .provider-actions {
        align-items: stretch;
        flex-direction: column;
    }

    .runner-provider-grid .provider-buttons {
        width: 100%;
        margin-left: 0;
    }

    .runner-provider-grid .provider-buttons .button {
        flex: 1;
    }

    .runner-select-row {
        align-items: stretch;
        flex-direction: column;
        gap: 8px;
    }

    .runner-select-row select {
        width: 100%;
        min-height: 44px;
    }

    .runner-secret-row {
        align-items: stretch;
        flex-direction: column;
    }

    .runner-secret-row .button {
        width: 100%;
    }

    .recommendation {
        align-items: stretch;
        flex-direction: column;
    }

    .route-row {
        grid-template-columns: 1fr;
        gap: 5px;
    }

    .route-row .badge {
        width: max-content;
    }

    .executor-row {
        align-items: stretch;
        flex-direction: column;
    }

    .executor-row .button {
        width: 100%;
    }
}

@media (prefers-reduced-motion: reduce) {
    *,
    *::before,
    *::after {
        scroll-behavior: auto !important;
        transition-duration: 0.01ms !important;
        animation-duration: 0.01ms !important;
    }
}
