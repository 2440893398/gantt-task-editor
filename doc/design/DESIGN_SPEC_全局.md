# 全局设计规范文档
## Global Design Specification Document

**文档版本**: v2.0
**创建时间**: 2026-01-11
**更新时间**: 2026-01-18
**适用范围**: 甘特图任务编辑工具全局
**技术栈**: Tailwind CSS 4 + DaisyUI 5

---

## 📑 目录

1. [设计概述](#1-设计概述)
2. [设计 Token 系统](#2-设计-token-系统)
3. [色彩系统](#3-色彩系统)
4. [字体规范](#4-字体规范)
5. [间距系统](#5-间距系统)
6. [圆角系统](#6-圆角系统)
7. [阴影系统](#7-阴影系统)
8. [动效规范](#8-动效规范)
9. [组件规范](#9-组件规范)
10. [图标系统](#10-图标系统)
11. [响应式设计](#11-响应式设计)
12. [可访问性规范](#12-可访问性规范)
13. [暗色主题](#13-暗色主题)
14. [命名规范](#14-命名规范)
15. [最佳实践](#15-最佳实践)

---

## 1. 设计概述

### 1.1 设计理念

| 原则 | 描述 | 实践指南 |
|:----:|:----:|:----:|
| **效率优先** | 减少用户操作步骤 | 最多 3 次点击完成核心任务 |
| **即时反馈** | 所有操作有明确反馈 | 操作后 100ms 内给予视觉响应 |
| **一致性** | 统一的视觉语言 | 复用 DaisyUI 组件，保持风格统一 |
| **可访问性** | 支持多种交互方式 | 键盘可操作，符合 WCAG 2.1 AA |
| **渐进增强** | 基础功能优先保证 | 核心功能无 JS 也可用 |

### 1.2 目标用户

| 用户类型 | 主要场景 | 关键需求 |
|:----:|:----:|:----:|
| 项目经理 | 进度监控、资源分配 | 全局视图、批量操作 |
| 产品经理 | 功能规划、版本管理 | 里程碑、依赖关系 |
| 开发团队 | 任务分配、进度更新 | 快速编辑、状态切换 |
| 团队领导 | 团队绩效、决策支持 | 数据导出、报表 |

### 1.3 设计原则优先级

```
可用性 > 可访问性 > 美观性 > 性能优化
```

---

## 2. 设计 Token 系统

### 2.1 Token 架构

```
设计 Token 分层:
┌─────────────────────────────────────┐
│  语义层 (Semantic Tokens)           │  ← 组件使用这一层
│  --color-primary, --color-error     │
├─────────────────────────────────────┤
│  别名层 (Alias Tokens)              │
│  --primary-color, --text-primary    │
├─────────────────────────────────────┤
│  基础层 (Primitive Tokens)          │
│  #4A90E2, #1F2937, 16px            │
└─────────────────────────────────────┘
```

### 2.2 CSS 变量与 Tailwind 映射

| CSS 变量 | Tailwind 类 | DaisyUI 类 | 用途 |
|:----:|:----:|:----:|:----:|
| `--primary-color` | `bg-primary` | `btn-primary` | 主色背景 |
| `--text-primary` | `text-gray-800` | `text-base-content` | 主要文字 |
| `--text-secondary` | `text-gray-500` | `text-base-content/60` | 次要文字 |
| `--border-color` | `border-gray-200` | `border-base-300` | 边框 |
| `--background-gray` | `bg-gray-50` | `bg-base-200` | 背景 |
| `--color-success` | `text-green-500` | `text-success` | 成功状态 |
| `--color-warning` | `text-amber-500` | `text-warning` | 警告状态 |
| `--color-error` | `text-red-500` | `text-error` | 错误状态 |

### 2.3 推荐使用方式

```html
<!-- ✅ 推荐: 使用 DaisyUI 组件类 -->
<button class="btn btn-primary">保存</button>

<!-- ✅ 可接受: 使用 Tailwind 工具类 -->
<div class="bg-gray-50 text-gray-800 p-4">内容</div>

<!-- ⚠️ 过渡期: 使用 CSS 变量 (逐步迁移) -->
<div style="color: var(--text-primary)">内容</div>

<!-- ❌ 避免: 硬编码颜色值 -->
<div style="color: #1F2937">内容</div>
```

---

## 3. 色彩系统

### 3.1 主色调

| 名称 | 色值 | CSS 变量 | Tailwind | 用途 |
|:----:|:----:|:----:|:----:|:----:|
| 主色 | `#4A90E2` | `--primary-color` | `primary` | 按钮、链接、强调 |
| 主色悬停 | `#3A7BC8` | `--primary-hover` | `primary-focus` | 交互状态 |
| 强调色 | `#50E3C2` | `--accent-color` | `accent` | 辅助装饰 |

### 3.2 语义色彩

| 状态 | 色值 | 浅色背景 | 对比度 | 用途 |
|:----:|:----:|:----:|:----:|:----:|
| 成功 | `#22C55E` | `#ECFDF5` | 4.5:1 ✓ | 完成、保存成功 |
| 警告 | `#F59E0B` | `#FFFBEB` | 3.1:1 ⚠️ | 需配合图标 |
| 错误 | `#EF4444` | `#FEF2F2` | 4.6:1 ✓ | 错误、删除 |
| 信息 | `#3B82F6` | `#EFF6FF` | 4.5:1 ✓ | 提示、帮助 |

> ⚠️ 警告色对比度较低，必须配合图标或加粗文字使用

### 3.3 中性色彩

| 名称 | 色值 | CSS 变量 | 用途 |
|:----:|:----:|:----:|:----:|
| 文字主色 | `#1F2937` | `--text-primary` | 标题、正文 |
| 文字次色 | `#6B7280` | `--text-secondary` | 辅助文字、占位符 |
| 文字禁用 | `#9CA3AF` | `--text-disabled` | 禁用状态文字 |
| 边框颜色 | `#E5E7EB` | `--border-color` | 边框、分割线 |
| 背景灰色 | `#F9FAFB` | `--background-gray` | 页面背景 |
| 悬停背景 | `#F3F4F6` | `--background-hover-gray` | 悬停状态 |
| 白色背景 | `#FFFFFF` | `--background-white` | 卡片、弹窗 |

### 3.4 渐变系统

```css
/* 主按钮渐变 */
.gradient-primary {
    background: linear-gradient(135deg, #3B82F6 0%, #2563EB 100%);
}

/* 弹窗头部渐变 */
.gradient-header {
    background: linear-gradient(135deg, #3B82F6 0%, #8B5CF6 100%);
}

/* 语义渐变 */
.gradient-success { background: linear-gradient(135deg, #22C55E 0%, #4ADE80 100%); }
.gradient-error   { background: linear-gradient(135deg, #EF4444 0%, #F87171 100%); }
.gradient-warning { background: linear-gradient(135deg, #F59E0B 0%, #D97706 100%); }
```

### 3.5 颜色使用规范

| 场景 | 正确做法 | 错误做法 |
|:----:|:----:|:----:|
| 错误提示 | 红色背景 + 红色边框 + 图标 | 仅红色文字 |
| 成功提示 | 绿色背景 + 绿色边框 + 图标 | 仅绿色文字 |
| 禁用按钮 | 灰色背景 + 灰色文字 | 降低透明度 |
| 链接 | 蓝色 + 下划线（悬停） | 仅颜色区分 |

---

## 4. 字体规范

### 4.1 字体族

```css
--font-family-base: 'Inter', 'Source Han Sans CN', -apple-system,
                    BlinkMacSystemFont, 'Segoe UI', sans-serif;
--font-family-mono: 'JetBrains Mono', 'Fira Code', 'Consolas', monospace;
```

### 4.2 字号系统

| 类型 | 字号 | 行高 | 字重 | Tailwind | 用途 |
|:----:|:----:|:----:|:----:|:----:|:----:|
| 标题 H1 | 18px | 1.4 | 600 | `text-lg font-semibold` | 弹窗标题 |
| 标题 H2 | 16px | 1.5 | 600 | `text-base font-semibold` | 面板标题 |
| 标题 H3 | 14px | 1.5 | 600 | `text-sm font-semibold` | 小标题 |
| 正文 | 14px | 1.6 | 400 | `text-sm` | 默认正文 |
| 辅助文字 | 13px | 1.5 | 400 | `text-xs` | 描述文字 |
| 小字 | 12px | 1.4 | 400 | `text-xs` | 标签、提示 |

### 4.3 行高规范

| 场景 | 行高 | 说明 |
|:----:|:----:|:----:|
| 紧凑型（标签） | 1.2 - 1.3 | 按钮、徽章 |
| 标准型（正文） | 1.5 - 1.6 | 段落、表单 |
| 宽松型（阅读） | 1.7 - 1.8 | 长文本 |

### 4.4 字重定义

```css
--font-weight-regular: 400;   /* 正文 */
--font-weight-medium: 500;    /* 强调正文、按钮 */
--font-weight-semibold: 600;  /* 标题 */
--font-weight-bold: 700;      /* 重要标题（少用） */
```

---

## 5. 间距系统

### 5.1 基础间距（8px 网格）

| Token | 值 | Tailwind | 用途 |
|:----:|:----:|:----:|:----:|
| `--spacing-xs` | 4px | `p-1` | 图标与文字间距 |
| `--spacing-sm` | 8px | `p-2` | 紧凑元素间距 |
| `--spacing-md` | 16px | `p-4` | 标准间距 |
| `--spacing-lg` | 24px | `p-6` | 区块间距 |
| `--spacing-xl` | 32px | `p-8` | 大区块间距 |
| `--spacing-2xl` | 48px | `p-12` | 页面级间距 |

### 5.2 组件内边距

| 组件类型 | 内边距 | Tailwind |
|:----:|:----:|:----:|
| 按钮（小） | `8px 16px` | `px-4 py-2` |
| 按钮（标准） | `10px 24px` | `px-6 py-2.5` |
| 按钮（大） | `12px 28px` | `px-7 py-3` |
| 表单控件 | `12px 14px` | `px-3.5 py-3` |
| 弹窗内容区 | `24px` | `p-6` |
| 面板头部 | `16px 20px` | `px-5 py-4` |
| 卡片 | `16px` | `p-4` |

### 5.3 元素间距

| 场景 | 间距 | 说明 |
|:----:|:----:|:----:|
| 表单字段之间 | 16px | 垂直堆叠 |
| 按钮组之间 | 8px | 水平排列 |
| 图标与文字 | 8px | 内联元素 |
| 段落之间 | 16px | 文本内容 |
| 区块之间 | 24px | 独立区域 |

---

## 6. 圆角系统

| Token | 值 | Tailwind | 用途 |
|:----:|:----:|:----:|:----:|
| `--radius-none` | 0 | `rounded-none` | 全屏元素 |
| `--radius-sm` | 4px | `rounded` | 标签、徽章 |
| `--radius-md` | 8px | `rounded-lg` | 按钮、输入框、卡片 |
| `--radius-lg` | 12px | `rounded-xl` | 面板 |
| `--radius-xl` | 16px | `rounded-2xl` | 弹窗 |
| `--radius-full` | 9999px | `rounded-full` | 头像、药丸标签 |

### 圆角使用规范

```
小元素 → 小圆角 (4px)
中等元素 → 中圆角 (8px)
大元素 → 大圆角 (12-16px)
```

---

## 7. 阴影系统

| Token | 值 | Tailwind | 用途 |
|:----:|:----:|:----:|:----:|
| `--shadow-sm` | `0 1px 2px rgba(0,0,0,0.05)` | `shadow-sm` | 输入框 |
| `--shadow-md` | `0 4px 6px -1px rgba(0,0,0,0.1)` | `shadow-md` | 卡片 |
| `--shadow-lg` | `0 10px 15px -3px rgba(0,0,0,0.1)` | `shadow-lg` | 下拉菜单 |
| `--shadow-xl` | `0 20px 25px -5px rgba(0,0,0,0.1)` | `shadow-xl` | 弹窗 |

### 特殊阴影

```css
/* 弹窗阴影 */
--shadow-modal: 0 25px 50px -12px rgba(0, 0, 0, 0.25);

/* 侧边面板阴影 */
--shadow-panel: -4px 0px 12px rgba(0, 0, 0, 0.1);

/* 悬浮卡片阴影 */
--shadow-hover: 0 4px 12px rgba(0, 0, 0, 0.15);

/* 聚焦环 */
--shadow-focus: 0 0 0 3px rgba(59, 130, 246, 0.1);
```

---

## 8. 动效规范

### 8.1 时长标准

| Token | 值 | 用途 |
|:----:|:----:|:----:|
| `--duration-instant` | 0ms | 无动画 |
| `--duration-fast` | 150ms | 悬停、焦点 |
| `--duration-normal` | 300ms | 展开、弹窗 |
| `--duration-slow` | 500ms | 页面切换 |

### 8.2 缓动函数

```css
--ease-default: cubic-bezier(0.4, 0, 0.2, 1);   /* 通用 */
--ease-in: cubic-bezier(0.4, 0, 1, 1);          /* 进入 */
--ease-out: cubic-bezier(0, 0, 0.2, 1);         /* 退出 */
--ease-bounce: cubic-bezier(0.68, -0.55, 0.265, 1.55); /* 弹性 */
```

### 8.3 常用动效

**弹窗动画:**
```css
/* 打开 */
@keyframes modal-in {
    from { opacity: 0; transform: scale(0.95); }
    to { opacity: 1; transform: scale(1); }
}
/* 关闭 */
@keyframes modal-out {
    from { opacity: 1; transform: scale(1); }
    to { opacity: 0; transform: scale(0.95); }
}
```

**面板滑入:**
```css
@keyframes slide-in-right {
    from { transform: translateX(100%); }
    to { transform: translateX(0); }
}
```

**Toast 提示:**
```css
@keyframes toast-in {
    from { opacity: 0; transform: translateX(-50%) translateY(-10px); }
    to { opacity: 1; transform: translateX(-50%) translateY(0); }
}
```

### 8.4 减少动画偏好

```css
@media (prefers-reduced-motion: reduce) {
    *, *::before, *::after {
        animation-duration: 0.01ms !important;
        transition-duration: 0.01ms !important;
    }
}
```

---

## 9. 组件规范

### 9.1 按钮组件

#### 按钮变体

| 变体 | DaisyUI 类 | 用途 |
|:----:|:----:|:----:|
| 主按钮 | `btn btn-primary` | 主要操作 |
| 次要按钮 | `btn btn-outline` | 次要操作 |
| 幽灵按钮 | `btn btn-ghost` | 轻量操作 |
| 危险按钮 | `btn btn-error` | 删除等危险操作 |
| 成功按钮 | `btn btn-success` | 确认、完成 |

#### 按钮尺寸

| 尺寸 | DaisyUI 类 | 内边距 | 字号 |
|:----:|:----:|:----:|:----:|
| 小 | `btn-sm` | `8px 16px` | 12px |
| 中 | `btn` | `10px 24px` | 14px |
| 大 | `btn-lg` | `12px 28px` | 16px |

#### 按钮状态

| 状态 | 类名 | 视觉表现 |
|:----:|:----:|:----:|
| 默认 | - | 基础样式 |
| 悬停 | `:hover` | 亮度 -10% |
| 按下 | `:active` | 缩放 0.98 |
| 聚焦 | `:focus-visible` | 焦点环 |
| 禁用 | `btn-disabled` | 透明度 50%, 禁止点击 |
| 加载 | `loading` | 显示 spinner |

```html
<!-- 示例 -->
<button class="btn btn-primary">保存</button>
<button class="btn btn-primary btn-disabled">保存</button>
<button class="btn btn-primary loading">保存中...</button>
```

### 9.2 表单控件

#### 输入框

```html
<!-- 基础输入框 -->
<input class="input input-bordered w-full" placeholder="请输入..." />

<!-- 带标签 -->
<label class="form-control w-full">
    <div class="label">
        <span class="label-text">任务名称</span>
    </div>
    <input class="input input-bordered" />
</label>

<!-- 错误状态 -->
<input class="input input-bordered input-error" />
<p class="text-error text-xs mt-1">请输入有效内容</p>
```

#### 选择框

```html
<select class="select select-bordered w-full">
    <option disabled selected>请选择</option>
    <option>选项1</option>
    <option>选项2</option>
</select>
```

#### 表单控件状态

| 状态 | 边框颜色 | 说明 |
|:----:|:----:|:----:|
| 默认 | `#E5E7EB` | 灰色边框 |
| 聚焦 | `#4A90E2` | 主色 + 焦点环 |
| 错误 | `#EF4444` | 红色边框 |
| 禁用 | `#E5E7EB` | 灰色背景 |

### 9.3 弹窗组件

#### 弹窗规格

| 类型 | 宽度 | 用途 |
|:----:|:----:|:----:|
| 小型 | 320px | 确认对话框 |
| 中型 | 420px | 配置弹窗 |
| 大型 | 640px | 编辑弹窗 |
| 超大 | 800px | 复杂表单 |

#### 弹窗结构

```html
<dialog class="modal">
    <div class="modal-box max-w-md">
        <!-- 头部 -->
        <div class="bg-gradient-to-r from-blue-500 to-purple-500 -m-6 mb-4 p-5 rounded-t-2xl">
            <h3 class="text-lg font-semibold text-white">弹窗标题</h3>
        </div>

        <!-- 内容区 -->
        <div class="py-4">
            <!-- 表单内容 -->
        </div>

        <!-- 底部操作 -->
        <div class="modal-action">
            <button class="btn btn-ghost">取消</button>
            <button class="btn btn-primary">确定</button>
        </div>
    </div>
    <form method="dialog" class="modal-backdrop">
        <button>close</button>
    </form>
</dialog>
```

### 9.4 侧边面板

| 属性 | 批量编辑面板 | 字段管理面板 |
|:----:|:----:|:----:|
| 宽度 (桌面) | 400px | 350px |
| 宽度 (移动) | 100% | 100% |
| Z-Index | 5000 | 6000 |
| 动画 | slide-in 0.3s | slide-in 0.25s |

### 9.5 Toast 提示

#### Toast 类型

| 类型 | 背景色 | 边框色 | 图标 |
|:----:|:----:|:----:|:----:|
| 成功 | `#ECFDF5` | `#22C55E` | ✓ |
| 错误 | `#FEF2F2` | `#EF4444` | ✕ |
| 警告 | `#FFFBEB` | `#F59E0B` | ⚠ |
| 信息 | `#EFF6FF` | `#3B82F6` | ℹ |

#### Toast 规范

- **位置**: 顶部居中，距顶 20px
- **宽度**: 自适应，最大 400px
- **显示时长**: 成功 2s，错误 4s，可手动关闭
- **Z-Index**: 99999

```html
<div class="toast toast-top toast-center z-[99999]">
    <div class="alert alert-success">
        <svg><!-- 图标 --></svg>
        <span>保存成功</span>
    </div>
</div>
```

### 9.6 标签/徽章

```html
<!-- 状态标签 -->
<span class="badge badge-success">完成</span>
<span class="badge badge-warning">进行中</span>
<span class="badge badge-error">延期</span>

<!-- 数字徽章 -->
<span class="badge badge-primary">5</span>
```

### 9.7 下拉菜单

```html
<div class="dropdown">
    <div tabindex="0" class="btn m-1">选项</div>
    <ul tabindex="0" class="dropdown-content menu bg-base-100 rounded-box w-52 p-2 shadow-lg">
        <li><a>选项 1</a></li>
        <li><a>选项 2</a></li>
    </ul>
</div>
```

---

## 10. 图标系统

### 10.1 图标尺寸

| 场景 | 尺寸 | Tailwind | 用途 |
|:----:|:----:|:----:|:----:|
| 行内 | 14px | `w-3.5 h-3.5` | 按钮内图标 |
| 标准 | 16px | `w-4 h-4` | 通用图标 |
| 中等 | 20px | `w-5 h-5` | 工具栏图标 |
| 大型 | 24px | `w-6 h-6` | 空状态图标 |

### 10.2 字段类型图标

| 类型 | 图标 | 颜色 | 描述 |
|:----:|:----:|:----:|:----:|
| 文本 | `Ā` | `#3B82F6` | 文本字段 |
| 数字 | `#` | `#10B981` | 数值字段 |
| 日期 | `📅` | `#8B5CF6` | 日期选择 |
| 下拉选择 | `▼` | `#6366F1` | 单选 |
| 多选 | `≡` | `#EC4899` | 复选 |

### 10.3 功能图标

| 功能 | 图标 | 说明 |
|:----:|:----:|:----:|
| 编辑 | ✏️ | 编辑操作 |
| 删除 | 🗑️ | 删除操作 |
| 关闭 | × | 关闭按钮 |
| 拖拽 | ⋮⋮ | 拖拽手柄 |
| 信息 | ⓘ | 帮助提示 |
| 成功 | ✓ | 完成状态 |
| 展开 | ▼ | 下拉展开 |
| 收起 | ▲ | 收起 |

### 10.4 图标使用规范

- **始终配合文字使用**，避免仅图标表意
- 图标与文字间距: 8px
- 可交互图标需有 hover 状态
- 功能图标需要 `aria-label`

---

## 11. 响应式设计

### 11.1 断点系统

| 断点 | 宽度 | Tailwind | 典型设备 |
|:----:|:----:|:----:|:----:|
| xs | < 475px | `max-sm:` | 小型手机 |
| sm | ≥ 640px | `sm:` | 大型手机 |
| md | ≥ 768px | `md:` | 平板竖屏 |
| lg | ≥ 1024px | `lg:` | 平板横屏/小桌面 |
| xl | ≥ 1280px | `xl:` | 桌面 |
| 2xl | ≥ 1536px | `2xl:` | 大屏幕 |

### 11.2 响应式策略

| 设备 | 布局策略 | 面板处理 |
|:----:|:----:|:----:|
| 移动端 (<768px) | 单栏布局 | 全屏覆盖 |
| 平板端 (768-1023px) | 侧边栏收起 | 面板宽 350px |
| 桌面端 (≥1024px) | 标准布局 | 面板宽 400px |

### 11.3 移动端适配要点

- 点击区域最小 44×44px
- 底部操作区避开安全区域
- 滑动手势支持
- 避免悬停依赖

### 11.4 滚动条样式

```css
/* Webkit 浏览器 */
::-webkit-scrollbar {
    width: 8px;
    height: 8px;
}

::-webkit-scrollbar-track {
    background: #E5E7EB;
    border-radius: 4px;
}

::-webkit-scrollbar-thumb {
    background: #D1D5DB;
    border-radius: 4px;
}

::-webkit-scrollbar-thumb:hover {
    background: #9CA3AF;
}
```

---

## 12. 可访问性规范

### 12.1 WCAG 2.1 AA 标准

| 要求 | 标准 | 检查项 |
|:----:|:----:|:----:|
| 颜色对比度 | 4.5:1 (正文) / 3:1 (大字) | 文字与背景 |
| 焦点可见 | 明显的焦点指示器 | 所有可交互元素 |
| 键盘可访问 | Tab 导航可达 | 所有功能 |
| 文本缩放 | 支持 200% 缩放 | 不截断内容 |

### 12.2 键盘操作

| 按键 | 操作 |
|:----:|:----:|
| `Tab` | 焦点前进 |
| `Shift + Tab` | 焦点后退 |
| `Enter` | 确认/激活 |
| `Space` | 切换/选择 |
| `Escape` | 取消/关闭 |
| `Arrow Keys` | 导航 |

### 12.3 焦点样式

```css
/* 焦点环样式 */
:focus-visible {
    outline: 2px solid var(--primary-color);
    outline-offset: 2px;
}

/* 表单控件焦点 */
.input:focus, .select:focus {
    border-color: var(--primary-color);
    box-shadow: 0 0 0 3px rgba(59, 130, 246, 0.1);
}
```

### 12.4 ARIA 使用

```html
<!-- 表单标签 -->
<input aria-label="任务名称" />
<input aria-describedby="name-error" aria-invalid="true" />

<!-- 动态内容 -->
<div aria-live="polite" aria-atomic="true">
    保存成功
</div>

<!-- 弹窗 -->
<dialog role="dialog" aria-modal="true" aria-labelledby="modal-title">
    <h2 id="modal-title">编辑任务</h2>
</dialog>
```

### 12.5 颜色不依赖

- 状态不仅用颜色表示，配合图标/文字
- 链接用下划线区分
- 错误用图标 + 红色 + 描述文字

---

## 13. 暗色主题

### 13.1 暗色主题变量

```css
[data-theme="dark"] {
    /* 背景 */
    --background-white: #1F2937;
    --background-gray: #111827;
    --background-hover-gray: #374151;

    /* 文字 */
    --text-primary: #F9FAFB;
    --text-secondary: #9CA3AF;
    --text-disabled: #6B7280;

    /* 边框 */
    --border-color: #374151;

    /* 主色调整 */
    --primary-color: #60A5FA;
    --primary-hover: #3B82F6;
}
```

### 13.2 暗色模式适配

| 元素 | 亮色 | 暗色 |
|:----:|:----:|:----:|
| 页面背景 | `#F9FAFB` | `#111827` |
| 卡片背景 | `#FFFFFF` | `#1F2937` |
| 文字主色 | `#1F2937` | `#F9FAFB` |
| 边框 | `#E5E7EB` | `#374151` |
| 阴影 | `rgba(0,0,0,0.1)` | `rgba(0,0,0,0.3)` |

### 13.3 系统主题检测

```css
@media (prefers-color-scheme: dark) {
    :root:not([data-theme="light"]) {
        /* 暗色主题变量 */
    }
}
```

---

## 14. 命名规范

### 14.1 CSS 类命名

| 类型 | 命名模式 | 示例 |
|:----:|:----:|:----:|
| 组件 | `.component-name` | `.modal-box`, `.form-control` |
| 状态 | `.is-*`, `.*-active` | `.is-loading`, `.btn-active` |
| 工具类 | Tailwind 约定 | `.flex`, `.mt-4` |
| 尺寸变体 | `*-sm`, `*-lg` | `.btn-sm`, `.input-lg` |

### 14.2 JS/TS 命名

| 类型 | 命名规则 | 示例 |
|:----:|:----:|:----:|
| 函数 | camelCase | `showToast()`, `handleSubmit()` |
| 组件 | PascalCase | `TaskEditor`, `GanttChart` |
| 常量 | UPPER_SNAKE | `MAX_TASKS`, `API_URL` |
| CSS 变量 | kebab-case | `--primary-color` |

### 14.3 文件命名

| 类型 | 命名规则 | 示例 |
|:----:|:----:|:----:|
| 组件文件 | kebab-case | `task-editor.js` |
| 样式文件 | kebab-case | `gantt-chart.css` |
| 配置文件 | 约定名称 | `tailwind.config.js` |

---

## 15. 最佳实践

### 15.1 组件使用优先级

```
1. DaisyUI 组件类 (btn, input, modal...)
   ↓
2. Tailwind 工具类 (flex, mt-4, text-lg...)
   ↓
3. CSS 变量 (var(--primary-color))
   ↓
4. 自定义 CSS (尽量避免)
```

### 15.2 常见问题处理

| 问题 | 解决方案 |
|:----:|:----:|
| 弹窗被遮挡 | 检查 z-index 层级 |
| 按钮点击无响应 | 检查是否被覆盖元素阻挡 |
| 样式不生效 | 检查类名优先级 |
| 动画卡顿 | 使用 transform 代替位置属性 |
| 移动端点击延迟 | 添加 `touch-action: manipulation` |

### 15.3 性能优化

- 优先使用 CSS 动画而非 JS 动画
- 避免过多阴影嵌套
- 使用 `will-change` 提示浏览器优化
- 大列表使用虚拟滚动

### 15.4 Do / Don't

| ✅ Do | ❌ Don't |
|:----:|:----:|
| 使用 DaisyUI 组件 | 重复造轮子 |
| 使用 CSS 变量 | 硬编码颜色值 |
| 移动优先设计 | 仅考虑桌面端 |
| 提供键盘支持 | 仅支持鼠标操作 |
| 状态用多种方式表示 | 仅用颜色区分 |
| 测试可访问性 | 忽略辅助技术用户 |

---

## 16. Z-Index 层级管理

| 层级 | Z-Index | 组件 |
|:----:|:----:|:----:|
| 基础内容 | 0-99 | 普通页面内容 |
| 图例/工具栏 | 100-999 | 固定位置元素 |
| 下拉菜单 | 1000-1999 | 弹出层 |
| 弹窗 | 2000-4999 | 模态对话框 |
| 侧边面板 | 5000-5999 | 批量编辑面板 |
| 高优先级面板 | 6000-6999 | 字段管理面板 |
| Toast | 99999 | 全局提示 |

---

## 17. CSS 变量完整参考

```css
:root {
    /* ===== 色彩系统 ===== */
    --primary-color: #4A90E2;
    --primary-hover: #3A7BC8;
    --accent-color: #50E3C2;

    --text-primary: #1F2937;
    --text-secondary: #6B7280;
    --text-disabled: #9CA3AF;

    --border-color: #E5E7EB;
    --background-gray: #F9FAFB;
    --background-hover-gray: #F3F4F6;
    --background-white: #FFFFFF;

    --color-success: #22C55E;
    --color-warning: #F59E0B;
    --color-error: #EF4444;
    --color-info: #3B82F6;

    /* ===== 字体规范 ===== */
    --font-family-base: 'Inter', 'Source Han Sans CN', -apple-system, sans-serif;
    --font-family-mono: 'JetBrains Mono', 'Consolas', monospace;

    --font-size-xs: 12px;
    --font-size-sm: 13px;
    --font-size-base: 14px;
    --font-size-lg: 16px;
    --font-size-xl: 18px;

    --font-weight-regular: 400;
    --font-weight-medium: 500;
    --font-weight-semibold: 600;
    --font-weight-bold: 700;

    --line-height-tight: 1.25;
    --line-height-normal: 1.5;
    --line-height-relaxed: 1.75;

    /* ===== 间距系统 ===== */
    --spacing-xs: 4px;
    --spacing-sm: 8px;
    --spacing-md: 16px;
    --spacing-lg: 24px;
    --spacing-xl: 32px;
    --spacing-2xl: 48px;

    /* ===== 圆角系统 ===== */
    --radius-none: 0;
    --radius-sm: 4px;
    --radius-md: 8px;
    --radius-lg: 12px;
    --radius-xl: 16px;
    --radius-full: 9999px;

    /* ===== 阴影系统 ===== */
    --shadow-sm: 0 1px 2px rgba(0, 0, 0, 0.05);
    --shadow-md: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
    --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
    --shadow-xl: 0 20px 25px -5px rgba(0, 0, 0, 0.1);
    --shadow-modal: 0 25px 50px -12px rgba(0, 0, 0, 0.25);
    --shadow-focus: 0 0 0 3px rgba(59, 130, 246, 0.1);

    /* ===== 过渡动画 ===== */
    --duration-instant: 0ms;
    --duration-fast: 150ms;
    --duration-normal: 300ms;
    --duration-slow: 500ms;

    --ease-default: cubic-bezier(0.4, 0, 0.2, 1);
    --ease-in: cubic-bezier(0.4, 0, 1, 1);
    --ease-out: cubic-bezier(0, 0, 0.2, 1);

    /* ===== 组件尺寸 ===== */
    --modal-sm: 320px;
    --modal-md: 420px;
    --modal-lg: 640px;
    --modal-xl: 800px;

    --panel-width: 400px;
    --panel-width-sm: 350px;

    /* ===== Z-Index 层级 ===== */
    --z-base: 0;
    --z-dropdown: 1000;
    --z-modal: 2000;
    --z-panel: 5000;
    --z-panel-high: 6000;
    --z-toast: 99999;
}
```

---

**文档状态**: ✅ 已完成 v2.0
**维护责任**: UI/UX 设计团队
**更新频率**: 随项目迭代更新
**技术审核**: 前端开发团队

---

## 版本历史

| 版本 | 日期 | 变更内容 |
|:----:|:----:|:----:|
| v2.0 | 2026-01-18 | 增加 Tailwind/DaisyUI 映射、完善组件规范、增强可访问性指南 |
| v1.0 | 2026-01-11 | 初始版本 |
