# 优化
- 1.右下角的视图类型切换，交互有点反直觉，点减号是往右切换，点加号是往左切换。调整一下
![alt text](image.png)
- 2.菜单栏的试图切换按钮去掉，这个按钮和右下角的切换聚合到一起。用户可以通过操作+、-来切换，也可以直接通过下拉选择
- 3.菜单栏中的今日操作，是一个按钮，当前的样式不像一个按钮，并将这个按钮放在右边
- 4.菜单栏中的导出，是导出excel,不是导出json
- 5.导出和导入都去掉任务ID和父级任务ID，为了方便用户导入，增加一个层级字段，放在第一个。这个字段支持的形式有【层级编号、路径表示法】
路径表示法
用斜杠路径表示完整层级 
/Project #1 
/Project #1/Task #1   
/Project #1/Task #2
层级缩进法
1 → 顶级节点
1.1 → 父节点是 1
1.1.1 → 父节点是 1.1
# bug
- 1.excel导出字段的顺序和表格视图中的不一致
- 2.固定列的列名也需要做本地化处理
